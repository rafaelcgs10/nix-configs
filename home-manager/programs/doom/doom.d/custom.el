;;; custom.el --- unused -*- lexical-binding: t; -*-
;;
;; This file is intentionally empty. `custom-file' is redirected to the Doom
;; cache dir in config.el (see the "Settings migrated out of custom.el"
;; section), so Doom does NOT load this file and `M-x customize' does NOT write
;; to it. Put real settings in config.el instead.
